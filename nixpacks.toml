[phases.setup]
nixPkgs = ["nodejs_22", "python3", "gnumake", "gcc"]

[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"

